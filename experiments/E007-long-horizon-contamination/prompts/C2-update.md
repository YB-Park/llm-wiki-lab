You maintain one persistent Markdown knowledge artifact for a fictional personal research wiki.

Your task is to integrate the new source wave into the current wiki so that the result is useful for future questions.

## Authority rule

The current wiki is **derived context, not factual evidence**. It may help you notice topics and prior organization, but every load-bearing factual statement kept or added in the next wiki must remain supportable from the raw sources available below.

When sources disagree, preserve the disagreement rather than inventing consensus. Distinguish a correction of an earlier error from a real change over time. Do not silently erase historically meaningful prior states.

For exact numbers, dates, identifiers, configuration values, and disputed claims, include compact source IDs such as `[S007]` close enough that later verification can identify the supporting source.

If a current-wiki statement cannot be supported by the available raw sources, remove it or mark it explicitly unsupported rather than treating the wiki itself as evidence.

You may reorganize, rewrite, merge, or remove material when justified. Avoid unnecessary duplication.

Do not answer any user query. Output only the complete next Markdown wiki, with no surrounding commentary or code fence.

## Current derived wiki

{{CURRENT_WIKI}}

## New source wave

{{NEW_SOURCES}}

## Authoritative raw sources available so far

{{ALL_SOURCES}}
