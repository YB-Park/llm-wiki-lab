# Enable one workspace at a time

**Set Up Project Memory** is the explicit opt-in boundary for the current workspace.

Setup creates or verifies LLM Wiki's private local store, checks that the store is protected from Git, and then makes project memory available to the Agent.

Setup itself makes **no model call**.
