# Remote S1 candidate publication request

This marker requests publication of the next successful `VS Code Dogfood` artifact from PR #222 into this directory.

The candidate publisher lives on `main` and is intentionally separate from the stable/versioned Dogfood publisher. It must not modify `dogfood/releases/llm-wiki-dogfood-latest.vsix` or any versioned stable VSIX before installed Remote-SSH dogfood is earned.
